 # 4. Replace the double space from problem 3 with single spaces. 

name = "hasnain  nisar"
name = name.replace("  ", " ")  # Remember strings are immutable, so the string name is not changed but a new #  string is formed called name.

print(name)
